# Port the mock webhook server listens on
PORT=3000

# Optional: shared secret to validate incoming Vapi webhook requests
# (check this against a header you configure in Vapi's tool server settings)
VAPI_WEBHOOK_SECRET=replace_me_with_a_random_secret

# Optional: log verbosity (info | debug)
LOG_LEVEL=info
